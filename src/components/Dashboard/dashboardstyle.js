import styled from 'styled-components';

export  const StyleDiv = styled.div`
    margin:2rem 2rem;
    border:2px solid;
    cursor:pointer;
    &:hover{
        background-color:lightblue;
        color:#ffff;
    }
`; 